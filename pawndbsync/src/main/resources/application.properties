# Application
spring.application.name=pawndbsync
server.port=8081

# Give the scheduler 2 threads so DB sync and file sync run in parallel
spring.task.scheduling.pool.size=2

# Cloud (Railway) PostgreSQL — override in C:\PawnDbSync\application.properties
cloud.datasource.url=jdbc:postgresql://REPLACE_CLOUD_HOST:PORT/DB
cloud.datasource.username=postgres
cloud.datasource.password=REPLACE_CLOUD_PASSWORD

# Local PostgreSQL — override in C:\PawnDbSync\application.properties
local.datasource.url=jdbc:postgresql://localhost:5432/pawnbroking
local.datasource.username=postgres
local.datasource.password=REPLACE_LOCAL_PASSWORD

# AWS S3 — file sync — override in C:\PawnDbSync\application.properties
aws.accessKey=REPLACE_AWS_ACCESS_KEY
aws.secretKey=REPLACE_AWS_SECRET_KEY
aws.region=eu-north-1
aws.s3.bucket=REPLACE_BUCKET_NAME
aws.s3.prefix=alwarpuram

# Disable Spring Boot's auto datasource (we configure both manually)
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\
  org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
